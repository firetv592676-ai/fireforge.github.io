const state={tab:'apps',query:'',items:[]};const grid=document.querySelector('#grid'),empty=document.querySelector('#empty'),count=document.querySelector('#count'),title=document.querySelector('#title');document.querySelector('#year').textContent=new Date().getFullYear();const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function render(){const q=state.query.toLowerCase();const items=state.items.filter(x=>(x.category===state.tab)&&`${x.name} ${x.description}`.toLowerCase().includes(q));title.textContent=state.tab==='apps'?'Apps & utilities':'Exploits & security research';count.textContent=`${items.length} item${items.length===1?'':'s'}`;empty.hidden=!!items.length;grid.innerHTML=items.map(x=>`<article class="card"><div class="cover">${x.iconPath?`<img src="${esc(x.iconPath)}" alt="">`:`<div class="fallback">${x.category==='exploit'?'🛡️':'📦'}</div>`}</div><div class="body"><span class="kicker">${esc(x.type||x.category)}</span><h3>${esc(x.name)}</h3><p class="desc">${esc(x.description||'No description provided.')}</p><div class="meta">${esc(x.version||'')} ${esc(x.size||'')}</div><a class="download" target="_blank" href="${esc(x.downloadUrl||`https://drive.google.com/file/d/${x.driveId}/view`)}">Open download ↗</a></div></article>`).join('')}fetch(`./data/catalog.json?${Date.now()}`).then(r=>r.json()).then(x=>{state.items=x;render()}).catch(()=>render());document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{state.tab=b.dataset.tab;document.querySelectorAll('.tab').forEach(t=>t.classList.toggle('active',t===b));render()});document.querySelector('#search').oninput=e=>{state.query=e.target.value;render()};
