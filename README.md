# FireForge
Upload these files to the root of your GitHub Pages repository. Replace `REPLACE_WITH_APPS_SCRIPT_URL` in `index.html` with your Apps Script Web App URL. Put `apps-script/Code.gs` into Google Apps Script, configure the PIN and GitHub Script Properties, deploy as a Web App, then run `setupAutomation()` once.
